name: Build Agent

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build
        run: cargo build --release --target x86_64-pc-windows-msvc

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: agent-windows
          path: |
            target/x86_64-pc-windows-msvc/release/agent.exe
            target/x86_64-pc-windows-msvc/release/uninstall.exe

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust + musl target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install musl tools
        run: sudo apt-get install -y musl-tools

      - name: Build (static musl)
        run: cargo build --release --target x86_64-unknown-linux-musl

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: agent-linux
          path: |
            target/x86_64-unknown-linux-musl/release/agent
            target/x86_64-unknown-linux-musl/release/uninstall

  release:
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: "Release v${{ github.run_number }}"
          files: |
            dist/agent-windows/agent.exe
            dist/agent-windows/uninstall.exe
            dist/agent-linux/agent
            dist/agent-linux/uninstall